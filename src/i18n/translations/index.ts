import en from './en.json'
import ms from './ms.json'
import zh_CN from './zh_CN.json'

// ms translation is done by google translate, for testing purposes
const Translations = {
    en,
    ms,
    zh_CN
};

export default Translations;
